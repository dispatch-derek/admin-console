Dark-fill select with custom caret; matches Input. Pass `options` or raw `<option>` children.

```jsx
<Select label="Role" options={[{value:"default",label:"Default"},{value:"admin",label:"Administrator"}]} />
```
