Rounded status pill for event logs / metadata. Tone maps to the product's event colors.

```jsx
<Badge tone="success">login_event</Badge>
<Badge tone="danger">user_deleted</Badge>
```

Tones: `info` (blue), `success` (green), `warn` (yellow), `danger` (red), `neutral`.
