Section header that opens every admin console page — bold title, small description, 2px bottom rule, optional right-aligned action.

```jsx
<PageHeader title="Users"
  description="All accounts on this instance."
  action={<Button variant="cta">Add user</Button>} />
```
